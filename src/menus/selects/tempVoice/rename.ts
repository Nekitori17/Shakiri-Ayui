import { SelectMenuInterface } from "../../../types/InteractionInterfaces";

const choice: SelectMenuInterface = {
  execute(interaction, client) {
    
  },
  id: "temp-voice-rename",
  disabled: false,
  voiceChannel: true,
}